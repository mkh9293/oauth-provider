server:
  port: 8084
  servlet:
    context-path: /developers

spring:
  jpa:
    database: mysql
  datasource:
    url: jdbc:mysql://localhost:3306/oauth?serverTimezone=UTC&useSSL=false
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
